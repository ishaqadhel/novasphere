<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} - Novasphere</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body>
  <div class="d-flex min-vh-100">
    <!-- Sidebar -->
    <div class="bg-dark text-white p-3 flex-shrink-0" style="width: 250px; min-width: 250px;">
      <h4 class="mb-4">Novasphere</h4>
      <ul class="nav nav-pills flex-column">
        <li class="nav-item"><a href="/app/dashboard" class="nav-link text-white"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
        <li class="nav-item"><a href="/app/material-category" class="nav-link text-white"><i class="bi bi-folder"></i> Material Categories</a></li>
        <li class="nav-item"><a href="/app/material" class="nav-link text-white"><i class="bi bi-box-seam"></i> Materials</a></li>
        <li class="nav-item"><a href="/app/project" class="nav-link text-white active"><i class="bi bi-building"></i> Projects</a></li>
        <li class="nav-item">
          <a href="/app/supplier" class="nav-link text-white">
            <i class="bi bi-shop"></i> Suppliers
          </a>
        </li>
        <li class="nav-item">
          <a href="/app/report" class="nav-link text-white">
            <i class="bi bi-graph-up"></i> Reports
          </a>
        </li>
        <li class="nav-item">
          <a href="/app/user" class="nav-link text-white">
            <i class="bi bi-people"></i> Users
          </a>
        </li>
        <li class="nav-item">
          <a href="/pricing" class="nav-link text-white">
            <i class="bi bi-tag"></i> Pricing
          </a>
        </li>
      </ul>
      <hr class="text-white">
      <div class="mt-auto pt-4">
        <p class="mb-1"><small>{{ user.first_name }} {{ user.last_name }}</small></p>
        <p class="mb-1"><small class="text-muted">{{ user.email }}</small></p>
        <form action="/auth/logout" method="POST" class="mt-2"><button type="submit" class="btn btn-sm btn-outline-light w-100"><i class="bi bi-box-arrow-right"></i> Logout</button></form>
      </div>
    </div>

    <!-- Main Content -->
    <div class="flex-grow-1" style="min-width: 0;">
      <nav class="navbar navbar-light bg-light border-bottom px-4">
        <div class="container-fluid">
          <div class="d-flex align-items-center">
            <a href="/app/project/{{ project.project_id }}" class="btn btn-outline-secondary btn-sm me-3"><i class="bi bi-arrow-left"></i> Back to Project</a>
            <div>
              <h5 class="mb-0">{{ project.name }} - Materials</h5>
              <small class="text-muted">Material Requirements Management</small>
            </div>
          </div>
          <span class="navbar-text">Welcome, {{ user.first_name }}!</span>
        </div>
      </nav>

      <div class="p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h4>Project Materials</h4>
          <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#addReqCard">
            <i class="bi bi-plus-circle"></i> Add Material Requirement
          </button>
        </div>

        <div class="collapse mb-4" id="addReqCard">
          <div class="card card-body bg-light border-0 shadow-sm">
            <h5 class="card-title mb-3">Assign Material to Project</h5>
            
            <form hx-post="/app/project/{{ project.project_id }}/materials" 
                  hx-target="#pmr-table-body" 
                  hx-swap="afterbegin"
                  hx-on::after-request="this.reset(); new bootstrap.Collapse(document.getElementById('addReqCard')).hide();">
              
              <div class="row g-3">
                <div class="col-md-4">
                  <label class="form-label small text-muted">Material</label>
                  <select name="material_id" class="form-select" required>
                    <option value="">Select Material...</option>
                    {{#materials}}
                      <option value="{{material_id}}">{{name}}</option>
                    {{/materials}}
                  </select>
                </div>

                <div class="col-md-4">
                  <label class="form-label small text-muted">Supplier</label>
                  <select name="supplier_id" class="form-select" required>
                    <option value="">Select Supplier...</option>
                    {{#suppliers}}
                      <option value="{{supplier_id}}">{{name}}</option>
                    {{/suppliers}}
                  </select>
                </div>

                <div class="col-md-2">
                  <label class="form-label small text-muted">Quantity</label>
                  <input type="number" name="quantity" class="form-control" required min="1">
                </div>

                <div class="col-md-2 d-flex align-items-end">
                  <button type="submit" class="btn btn-success w-100">
                    <i class="bi bi-check-lg"></i> Add
                  </button>
                </div>
              </div>
            </form>
          </div>
        </div>

        <div class="card shadow-sm">
          <div class="card-body p-0">
            <div class="table-responsive">
              <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                  <tr>
                    <th class="ps-4">Material</th>
                    <th>Supplier</th>
                    <th>Quantity</th>
                    <th>Status</th>
                    <th class="text-end pe-4">Actions</th>
                  </tr>
                </thead>
                <tbody id="pmr-table-body">
                  {{#requirements}}
                    <tr>
                      <td class="ps-4">{{material_name}}</td>
                      <td class="text-muted">{{supplier_name}}</td>
                      <td class="fw-bold">{{quantity}}</td>
                      <td>
                        {{#status_name}}
                          <span class="badge bg-info bg-opacity-10 text-info px-2 py-1">{{status_name}}</span>
                        {{/status_name}}
                        {{^status_name}}
                          <span class="badge bg-secondary bg-opacity-10 text-secondary">Pending</span>
                        {{/status_name}}
                      </td>
                      <td class="text-end pe-4">
                        <div class="btn-group btn-sm">
                          <button class="btn btn-outline-warning"
                            hx-get="/app/project/{{project_id}}/materials/edit/{{project_material_requirement_id}}"
                            hx-target="closest tr"
                            hx-swap="outerHTML">
                            <i class="bi bi-pencil"></i>
                          </button>

                          <button class="btn btn-outline-danger"
                            hx-post="/app/project/{{project_id}}/materials/{{project_material_requirement_id}}/delete"
                            hx-target="closest tr"
                            hx-swap="outerHTML"
                            hx-confirm="Are you sure you want to remove this requirement?">
                            <i class="bi bi-trash"></i>
                          </button>
                        </div>
                      </td>
                    </tr>
                  {{/requirements}}
                  
                  {{^requirements}}
                    <tr>
                      <td colspan="5" class="text-center text-muted py-5">
                        <i class="bi bi-clipboard-data h2 d-block mb-2"></i>
                        No material requirements found for this project
                      </td>
                    </tr>
                  {{/requirements}}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
