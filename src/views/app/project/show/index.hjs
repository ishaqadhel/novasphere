<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} - Novasphere</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body>
  <div class="d-flex min-vh-100">
    
    <!-- Sidebar: Fixed Width -->
    <div class="bg-dark text-white p-3 flex-shrink-0" style="width: 250px;">
      <h4 class="mb-4">Novasphere</h4>
      <ul class="nav nav-pills flex-column">
        <li class="nav-item"><a href="/app/dashboard" class="nav-link text-white"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
        <li class="nav-item"><a href="/app/material-category" class="nav-link text-white"><i class="bi bi-folder"></i> Material Categories</a></li>
        <li class="nav-item"><a href="/app/material" class="nav-link text-white"><i class="bi bi-box-seam"></i> Materials</a></li>
        <li class="nav-item"><a href="/app/project" class="nav-link active bg-primary text-white"><i class="bi bi-building"></i> Projects</a></li>
        <li class="nav-item">
          <a href="/app/supplier" class="nav-link text-white">
            <i class="bi bi-shop"></i> Suppliers
          </a>
        </li>
        <li class="nav-item">
          <a href="/app/report" class="nav-link text-white">
            <i class="bi bi-graph-up"></i> Reports
          </a>
        </li>
        <li class="nav-item"><a href="/app/user" class="nav-link text-white"><i class="bi bi-people"></i> Users</a></li>
        <li class="nav-item">
          <a href="/pricing" class="nav-link text-white">
            <i class="bi bi-tag"></i> Pricing
          </a>
        </li>
      </ul>
      <hr class="text-white">
      <div class="mt-auto pt-4">
        <p class="mb-1"><small>{{ user.first_name }} {{ user.last_name }}</small></p>
        <p class="mb-1"><small class="text-muted">{{ user.email }}</small></p>
        <form action="/auth/logout" method="POST" class="mt-2"><button type="submit" class="btn btn-sm btn-outline-light w-100"><i class="bi bi-box-arrow-right"></i> Logout</button></form>
      </div>
    </div>

    <!-- Main Content -->
    <div class="flex-grow-1" style="min-width: 0;">
      <nav class="navbar navbar-light bg-light border-bottom px-4">
        <div class="container-fluid">
          <div class="d-flex align-items-center">
            <a href="/app/project" class="btn btn-outline-secondary btn-sm me-3"><i class="bi bi-arrow-left"></i></a>
            <div>
              <h5 class="mb-0">{{ project.name }}</h5>
              <small class="text-muted">Project Dashboard</small>
            </div>
          </div>
          <div>
            <a href="/app/project/{{ project.project_id }}/edit" class="btn btn-outline-warning btn-sm me-1"><i class="bi bi-pencil"></i> Edit Details</a>
          </div>
        </div>
      </nav>

      <div class="p-4">
        
        <!-- Project Summary Cards -->
        <div class="row g-4 mb-5">
          <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body">
                <h6 class="text-muted mb-2">Current Stage</h6>
                <h4 class="mb-2"><span class="badge {{ project.status_badge_class }}">{{ project.status_name }}</span></h4>
                <div class="small text-muted">
                  {{#project.is_active_bool}}<i class="bi bi-check-circle-fill text-success"></i> Active{{/project.is_active_bool}}
                  {{^project.is_active_bool}}<i class="bi bi-x-circle-fill text-secondary"></i> Inactive{{/project.is_active_bool}}
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body">
                <h6 class="text-muted mb-2">Total Budget</h6>
                <h3 class="mb-0 text-dark fw-bold">{{ project.formatted_budget }}</h3>
                <small class="text-muted">Estimated Cost</small>
              </div>
            </div>
          </div>
          <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body">
                <h6 class="text-muted mb-2">Project Manager</h6>
                <div class="d-flex align-items-center">
                  <div class="bg-light rounded-circle p-2 me-2"><i class="bi bi-person fs-5"></i></div>
                  <div>
                    <div class="fw-bold">{{ project.manager_name }}</div>
                    <small class="text-muted">{{ project.manager_email }}</small>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="col-md-3">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body">
                <h6 class="text-muted mb-2">Timeline</h6>
                <div class="small">
                  <div class="d-flex justify-content-between mb-1"><span>Start:</span> <span class="fw-bold">{{ project.formatted_start }}</span></div>
                  <div class="d-flex justify-content-between mb-1"><span>Est. End:</span> <span class="fw-bold">{{ project.formatted_end }}</span></div>
                  {{#project.formatted_actual_end}}
                  <div class="d-flex justify-content-between text-success"><span>Actual End:</span> <span class="fw-bold">{{ project.formatted_actual_end }}</span></div>
                  {{/project.formatted_actual_end}}
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Project Modules -->
        <h5 class="mb-3 text-secondary">Project Modules</h5>
        <div class="row g-4">
          
          <!-- TASK MANAGEMENT MODULE -->
          <div class="col-md-6">
            <div class="card border-primary h-100 shadow-sm hover-card">
              <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-start">
                  <div>
                    <h5 class="card-title text-primary fw-bold mb-3">Task Management</h5>
                    <p class="text-muted mb-4">Manage project tasks, track progress, assign responsibilities, and monitor deadlines.</p>
                    
                    <a href="/app/project-task?project_id={{ project.project_id }}" class="btn btn-primary">
                      <i class="bi bi-list-check me-2"></i> Manage Tasks
                    </a>
                  </div>
                  <div class="bg-primary bg-opacity-10 p-3 rounded-3"><i class="bi bi-kanban fs-1 text-primary"></i></div>
                </div>
              </div>
            </div>
          </div>

          <!-- MATERIAL MANAGEMENT MODULE -->
          <div class="col-md-6">
            <div class="card border-success h-100 shadow-sm hover-card">
              <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-start">
                  <div>
                    <h5 class="card-title text-success fw-bold mb-3">Material Management</h5>
                    <p class="text-muted mb-4">Track material usage, monitor inventory for this project, and manage procurement.</p>
                    <a href="/app/project/{{ project.project_id }}/materials" class="btn btn-success">
                      <i class="bi bi-box-seam me-2"></i> Manage Materials
                    </a>
                  </div>
                  <div class="bg-success bg-opacity-10 p-3 rounded-3"><i class="bi bi-cart4 fs-1 text-success"></i></div>
                </div>
              </div>
            </div>
          </div>

        </div>
        
        <!-- Description -->
        <div class="row mt-5">
          <div class="col-12">
            <div class="card border-0 shadow-sm">
              <div class="card-body">
                <h6 class="text-muted mb-3">Project Description</h6>
                <p class="mb-0 text-secondary" style="white-space: pre-line;">{{ project.description }}</p>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <style>.hover-card { transition: transform 0.2s; } .hover-card:hover { transform: translateY(-5px); }</style>
</body>
</html>